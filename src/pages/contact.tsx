import Layout from "components/Layout";
import styles from "../styles/Contact.module.css"

export default function Contact(){
    return(
        <Layout pageTitle="Contact Page">
            <h1 className={styles['subTitle']}>Contact Page</h1>
        </Layout>
    )
}